# hello-practice

A tiny practice project for learning the Git/GitHub pull request workflow.

## What it does

`greet.py` contains a single function, `greet(name)`, which returns a
friendly greeting string.

## Usage

```bash
python greet.py
```
